# Obsidian → GitHub Sync

Sync your Obsidian vault to a GitHub repository with automatic wiki-link to markdown conversion.

## Installation

```bash
uv pip install -e .

```

## Usage

```bash
# Run sync once
python3 -m obsidian_sync.cli sync

```
